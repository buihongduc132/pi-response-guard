{
  "name": "pi-response-guard",
  "version": "0.1.0",
  "description": "Pi extension that auto-recovers from empty, errored, or interrupted model responses",
  "main": "index.ts",
  "types": "dist/index.d.ts",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi",
    "extension",
    "retry",
    "error-recovery",
    "empty-response",
    "pi-coding-agent"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:ci": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": ">=2026.0.0"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "latest",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  },
  "license": "MIT"
}
